// ── SAKURA KITCHEN CIS 品牌色（單一來源）──
// 供元件 inline style={{}} 使用；Tailwind 類別（text-gold / bg-ink-80 / text-brand-red…）
// 由 src/styles/globals.css 的 CIS tokens + @theme inline 暴露，兩處數值必須一致。
export const GOLD = '#C9AA79';        // PANTONE 466c / Varnished 871c（品牌金）
export const GOLD_STRONG = '#B8965F'; // 金-壓深（hover / 金屬漸層深端；衍生自 GOLD）
export const GOLD_SOFT = '#D8C29A';   // 金-提亮（金屬漸層淺端；衍生自 GOLD）
export const INK = '#000000';         // PANTONE Black C（主黑：標題/內文）
export const INK_80 = '#3E3A39';      // PANTONE Black C 80%（深色面/次要文字）
export const BRAND_RED = '#F5333F';   // PANTONE Red 032c（強調/錯誤）
